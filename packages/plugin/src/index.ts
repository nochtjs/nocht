// import { N } from '@nocht/core';
// import { isFunction } from '@nocht/shared';
/*

if (import.meta.vitest) {
    const { describe, it, expect } = await import('vitest');
    const { nocht } = await import('@nocht/core');

    describe('@nocht/plugin', () => {
        it('should return a Nocht instance', () => {
            const n = nocht([]);

            const plugin = definePlugin(n, {
                test() {
                    return true;
                }
            });

            expect(plugin).toBeInstanceOf(Nocht);
        });

        it('should add methods to original Nocht', () => {
            const n = nocht([]);

            definePlugin(n, {
                test() {
                    return true;
                }
            });

            expect(n.test()).toBe(true);
        });
    })
}

*/